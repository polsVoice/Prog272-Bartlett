describe("A Suite", function() {
  it("expects to get 9", function() {
    expect(numberGetter.getNine()).toBe(9);
  });
  function() {
	  it("expects to get 9", function() {
		expect(numberGetter.getEight()).toBe(9);
	  });
	}
});
